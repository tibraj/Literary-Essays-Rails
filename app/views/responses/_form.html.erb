<%= render partial: 'layouts/errors', locals: {object: @response} %>

<%= form_for(@response) do |f| %>
    <%= f.hidden_field :essay_id %>
    <%= f.label :content %>
    <%= f.text_area :content %>
    <%= f.submit %>
<% end %>